# =============================================================================
# SO-101 LeRobot — teleoperation, data logging & web monitor
# =============================================================================
# Install everything with:
#     make install            # LeRobot (from config.yaml) + the deps below
#
# This file lists only the PyPI deps for the web app, bridge and config loader.
# LeRobot itself is NOT listed here: the project targets 0.5.2 (never published
# to PyPI), so it is installed editable from your local checkout. `make install`
# reads that checkout path from a SINGLE source — `lerobot_src` in
# configs/config.yaml — and installs `<checkout>[hardware,feetech,dataset]`
# before installing this file. Set the path once in config.yaml; nothing here.
#
#   hardware : pyserial / pynput      feetech : feetech-servo-sdk
#   dataset  : datasets / pandas / pyarrow / torchcodec
#   (LeRobot also pulls torch, numpy and opencv-python-headless)
#
# `pip install -r requirements.txt` alone installs the PyPI deps WITHOUT LeRobot.
# =============================================================================

# --- Web monitor / app (scripts/monitor.py, scripts/app.py) -----------------
fastapi>=0.115
uvicorn[standard]>=0.30

# --- Bridge between teleop and app (scripts/bridge.py) ----------------------
pyzmq>=25

# --- Config loading (scripts/_config.py) ------------------------------------
PyYAML>=6.0

# NOTE: do NOT add `opencv-python` here — LeRobot installs
# `opencv-python-headless`, and having both causes import conflicts.
